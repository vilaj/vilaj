version: "3.9"

services:
  python_flask:
    build: .
    volumes:
      - python_flask:/app
    ports:
      - "35000:5000"
    restart: unless-stopped

  nodered_reisobot:
    image: nodered/node-red:latest
    volumes:
      - nodered_reisobot:/data
    ports:
      - "31880:1880"
    restart: unless-stopped

volumes:
  nodered_reisobot:
  python_flask:

networks:
  default:
    name: reisobot
    driver: bridge
